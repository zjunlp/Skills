# Output JSON Schema

## Structure
The final output is a single JSON object. Each top-level key is a GitHub repository ID, provided as a string. The value for each key is an object containing the 8 specified metrics.

## Root Object
