# Example Reference Format File (`format.md`)

This is an example of a reference format file that this skill might be instructed to use. The skill reads this to understand the required output structure.

## Structure
The file typically contains:
1.  A **title or header** describing the content.
2.  A **code block** denoting the target format (e.g., ` 